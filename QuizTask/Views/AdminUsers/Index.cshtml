@model IEnumerable<QuizTask.Models.AdminUser>

@{
    ViewData["Title"] = "Index";
    Layout = "_LayoutAdmin";
}

<div class="content-body">
    <!-- Zero configuration table -->
    <section id="basic-datatable">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <h5>Careel Level List</h5>
                        <a style="float:right" class="btn btn-primary" asp-action="Create">
                            <i class="bx bx-plus-medical"></i>
                        </a>
                    </div>
                    <div class="card-body card-dashboard">

                        <div class="table-responsive">
                            <table class="table zero-configuration">
                                <thead>
                                    <tr>
                                        <th>Reg. No</th>
                                        <th>Name</th>
                                        <th>Email</th>
                                        <th>Password</th>
                                        <th>Role</th>
                                        <th>Mobile No</th>
                                        <th>Status</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        <tr>
                                            <td>@item.User_Reg_No</td>
                                            <td>@item.User_Name</td>
                                            <td>@item.User_Email</td>
                                            <td>@item.User_Passward</td>
                                            <td>@item.Role</td>
                                            <td>@item.Mobile_No</td>

                                            <td>
                                                @if (item.User_Status == true)
                                                {
                                                    @Html.Raw("Active")
                                                }
                                                else
                                                {
                                                    @Html.Raw("Deactive")
                                                }
                                            </td>

                                            <td>
                                                <a asp-action="Edit" class="btn btn-success  btn-sm" asp-route-id="@item.UserId">Edit</a> 
                                                <a asp-action="Details" class="btn btn-info  btn-sm" asp-route-id="@item.UserId">Details</a> 
                                                <a asp-action="Delete" class="btn btn-danger  btn-sm" asp-route-id="@item.UserId">Delete</a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>

                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
    <!--/ Zero configuration table -->

</div>


